Dịch vụ nào sau đây cần thiết để cho dịch vụ AD hoạt động, Để phân giải tên miền (DNS)
Dịch vụ nào trong Windows Server cho phép lưu trữ và quản lý trang web? (IIS)
Lệnh nào hiển thị thông tin địa chỉ IP của server?   (ipconfig)
Tiện ích nào sau đây là một phương thức bảo mật truy cập từ xa tốt hơn telnet?   (SSH)
Để tạo bản ghi phân giải tên trong DNS, ta dùng loại bản ghi nào cho tên miền? ( A Record)
Trong Active Directory, FSMO role nào chịu trách nhiệm phân phối SID cho domain?   (RID Master)
Loại bản ghi DNS nào dùng để ánh xạ địa chỉ IP với hostname? ( PTR Record)
Giao thức nào được sử dụng bởi Group Policy để update chính sách?  ( SMB và RPC)
Bản ghi nào cho biết Mail server( MX)
Bản ghi chứa thông tin phục vụ cho quá trình phân giải ngược của hệ thống DNS  ( PTR)
Bản ghi quy định trên các Server( NS)
Công cụ làm việc với nhóm chính sách có thể truy cập loại tập tin nào trong central store? (ADMX)
Trong hệ thống Mail Server, giao thức kết nối giữa server to server là   ( SMTP)
Trong phần mềm mô phỏng Packet Tracer, sử dụng công cụ nào trong số các công cụ sau đây để tiến hành kiểm tra quá trình gửi và nhận giữa các thiết bị?   (Add Simple PDU)
Để truy cập từ xa vào Router Cisco qua SSH, bạn cần cấu hình gì?   (VTY lines và SSH)
Giao thức nào cho phép switch gửi bản tin tìm kiếm VLAN tự động?   ( VTP)
Router sử dụng giao thức định tuyến nào để tìm đường ngắn nhất?   ( OSPF)
Giao thức nào giúp các switch tránh vòng lặp mạng?   ( STP)
Giao thức nào sau đây hỗ trợ định tuyến liên mạng lớn và phức tạp? Giao thức nào cho phép định tuyến giữa các Autonomous System?   (BGP)
Giao thức định tuyến nào sử dụng thuật toán vector khoảng cách?  ( RIP)
Công nghệ nào cho phép người sử dụng kết nối đến một mạng LAN của công ty qua Internet thông qua một kênh được mã hóa an toàn? ( VPN)
Dịch vụ nào trong Windows Server quản lý người dùng, nhóm và máy tính trong mạng?  ( Active Directory)
Có thể chuyển server từ chế độ Server Core sang chế độ GUI mà không phải cài đặt lại hệ điều hành --- Bản Server Manager mới trong Windows Server 2012 R2 giúp việc quản trị từ xa dễ dàng hơn
Smart paging -- Guest Integration Services
(loại các mạng)
Nếu bạn muốn mở rộng một volume kiểu simple, và sau khi mở rộng nó vẫn thuộc kiểu simple, thì bạn chỉ có thể sử dụng không gian đĩa còn trống trên cùng một đĩa cứng vật lý. -- Bạn có thể mở rộng một volume kiểu simple sang các đĩa cứng khác, miễn là volume đó không phải là system volume hoặc boot volume.
Group có chứa thành viên được thiết lập là primary group. -- Tài khoản hiện tại không có quyền xóa các đối tượng.
Volume kiểu striped giúp nâng cao hiệu xuất xử lý so với volume kiểu simple. -- Nếu một trong các đĩa cứng thuộc volume bị hư, toàn bộ dữ liệu sẽ bị mất.
